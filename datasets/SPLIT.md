# 시간 분할 (Decision 1)

> `manifests/datasets.manifest.json`에서 **자동 생성**된다. 손으로 고치지 말고 `make datasets-docs`로 다시 만들 것.

## 컷오프: **2025-01-01**

평가 세트는 **이 날짜 이후에 공개된 CVE에서만** 뽑는다. CVE 텍스트는 웹 전역에 미러링되어 있어 무작위 분할로는 베이스 모델의 사전학습 오염을 막을 수 없다. 베이스 모델이 볼 수 없었던 날짜는 막을 수 있다.

Qwen2.5-7B-Instruct는 2024년 9월에 공개되었고 학습 컷오프는 정확히 문서화되어 있지 않다. 공개일보다 여유를 둔 날짜가 보수적인 선택이다.

## 학습은 컷오프 **이전** 데이터만 쓴다

"시간으로 분할한다"의 보수적 해석이며, 배포 상황(아직 존재하지 않던 CVE를 예측)을 그대로 모사한다. 컷오프 이후이지만 평가에 표집되지 않은 CVE **102,836건**은 학습에 넣지 않고 별도로 보류·집계한다. P5가 필요하면 이 결정을 다시 볼 수 있도록 숫자를 남긴다.

## 컷오프 이전 평가 세트: 2022–2023년

같은 크기의 평가 세트를 컷오프 이전 구간에서 따로 보류한다. **두 세트의 점수 차이가 이 파이프라인 자체의 오염 크기 추정치**이며, 각주가 아니라 1급 결과로 보고한다.

## 표집

- 기간별 5,000 CVE를 고정 시드의 안정 해시 순서로 표집한다 (입력 순서와 무관).
- 하나의 CVE는 하나의 분할 라벨만 갖고, 세 CVE 과제가 모두 같은 라벨을 쓴다 — 코드에서 검증한다.

## CVE 분할 집계

| 분할 | CVE 수 |
|---|---|
| 학습 (`train`) | 228,867 |
| 컷오프 이후·미사용 (`post_cutoff_unused`) | 102,836 |
| 제외 (`excluded`) | 53,255 |
| 평가(컷오프 이후) (`eval_post_cutoff`) | 5,000 |
| 평가(컷오프 이전) (`eval_pre_cutoff`) | 5,000 |

## ATT&CK 기법 분할 (STIX `created` 기준)

| 분할 | 기법 수 |
|---|---|
| 학습 (`train`) | 738 |
| 평가(컷오프 이후) (`eval_post_cutoff`) | 79 |
| 평가(컷오프 이전) (`eval_pre_cutoff`) | 79 |
| 제외 (`excluded`) | 22 |

기법은 수가 적어 컷오프 이후 기법 **전부**를 평가에 넣고 같은 수를 2022–2023에서 보류한다.

