# RLVR 시연 — 구조가 도는지에 대한 기록

> `runs/rlvr/manifest.json`, `reward_log.jsonl`, `reward_inspection.json`, `runs/compare_rlvr.json`에서 **자동 생성**된다. `python -m rlvr.render_report`로 다시 만든다.

## 이 문서가 주장하는 것과 주장하지 않는 것

**이 실행은 RLVR 구조가 처음부터 끝까지 돈다는 것을 시연한다. 성능 향상을 주장하지 않는다.** 스텝 200회, 시드 하나, 과제 하나다. 아래 수치가 좋아 보이더라도 그것은 관측이지 효과가 아니며, 짝지은 검정이 뒷받침하지 않는 한 향상으로 읽어서는 안 된다. 나빠 보이는 경우도 마찬가지로 그대로 적는다 — 구조가 도는 것을 보이는 일에는 **잘 돌지 않을 때 그것을 보고하는 것**이 포함된다.

## 프롬프트 풀의 출처

롤아웃 프롬프트는 `data/out/cve_to_cwe/train_cond2_domain.jsonl`에서 뽑았다. 이 파일은 **출발 체크포인트 Cond-2가 학습한 바로 그 파일**이다. 시드 `sec-llm-p6-rlvr-v1`로 4,096건을 22,776건 중에서 골랐다.

| 겹침 | 건수 |
|---|---|
| Cond-2 학습 파일 안에 있는 풀 프롬프트 | **4,096 / 4,096** |
| Cond-1 학습 파일 안에 있는 풀 프롬프트 | 4,096 / 4,096 |
| 평가 세트(컷오프 이후)와 겹침 | 0 |
| 평가 세트(컷오프 이전)와 겹침 | 0 |

두 가지가 따라 나온다.

- **아래 보상 곡선은 학습 데이터 위에서 측정된 것이다.** 출발 체크포인트가 이미 본 프롬프트이므로, 보상이 오르는 것은 일반화의 증거가 아니라 본 것에 더 확실해졌다는 관측이다. Cond-2는 자기 데이터의 85%를 소비했으므로 풀의 대부분은 실제로 본 것이지만, 어느 항목이었는지는 패킹 순서에서 복원되지 않는다.
- **평가 세트와는 한 건도 겹치지 않는다**(0건, 0건). 뒤의 평가 비교는 이 프롬프트들로 오염되지 않았다. 이것은 설계가 아니라 확인이며, 확인했기 때문에 적는다.

## 과제 선택 — 작업지시서가 든 근거는 틀렸다

P6 작업지시서는 `cve_to_cwe`를 고른 근거로 **Cond-0의 정확도**를 들었다. GRPO는 그룹이 전부 맞거나 전부 틀리면 어드밴티지가 0이 되므로 중간쯤의 정확도가 필요하고, Cond-0 기준으로는 이 과제만 그 조건을 만족한다. 근거 자체는 맞다 — **다만 잘못된 체크포인트의 정확도다.**

| | 컷오프 이후 | 컷오프 이전 |
|---|---|---|
| Cond-0 (작업지시서가 본 수치) | 39.0% | 51.1% |
| **Cond-2 (GRPO가 실제로 출발한 곳)** | **57.8%** | **66.1%** |

판단 기준이 되었어야 하는 것은 **출발 체크포인트가 실제로 롤아웃할 프롬프트에서 내는 정확도**다. 그 값은 이 실행의 처음 20스텝 롤아웃에서 **0.562**로 측정되었다(온도 1.0의 표본추출 기준). Cond-0의 수치보다 높고, 학습 데이터라는 점까지 더하면 그룹이 전부-정답으로 붕괴할 여지가 그만큼 크다.

**이 오류는 작업지시서에서 왔고, 구현은 그대로 따랐다.** `docs/engineering-rules.md` 규칙 6이 기록한 것과 같은 형태다 — 지시서에서 온 결함도 결함이며, 지시서를 따랐다는 것은 변명이 되지 않는다. 아래 그룹 붕괴 수치는 이 선택의 직접적 귀결이다.

## 설정

| 항목 | 값 |
|---|---|
| 과제 | `cve_to_cwe` |
| 출발 체크포인트 | `checkpoints/cond2/merged` |
| 스텝 | 200 / 200 |
| 그룹 크기 | 8 |
| 스텝당 생성 수 | 64 (프롬프트 8개) |
| 온도 / top-p | 1.0 / 1.0 |
| 학습률 / KL 계수 | 1e-05 / 0.0 |
| LoRA | `r`=32, `alpha`=64 |
| 생성 길이 상한 | 112 — 8x the longest target in the pool (14 tokens) |
| 프롬프트 풀 | 4,096 / 22,776건, 시드 `sec-llm-p6-rlvr-v1` |
| 소요 | 1.02시간 (the resumed segment only, from the chain's own markers. The first 100 steps ran in an attempt the host rebooted, and their elapsed time was not recorded anywhere that survived.) |

## 검증자

보상은 결정론적 검사 두 개의 합이다 — 스키마 유효성과 `cwe_id` 완전 일치, 각각 0에서 1.

**컨테이너도 컴파일러도 서브프로세스도 쓰지 않는다**(`subprocess_used: false`). CVSS 샌드박스는 규격의 공식을 실제로 실행해야 하는 평가 쪽에 있고, 거기서는 수만 건을 한 번 채점한다. 보상 루프는 스텝마다 수십 건을 채점하므로 항목당 1초짜리 검증자를 쓰면 실행이 끝나지 않는다.

스키마 검사는 `eval/scorers/schema.py`를 **수정 없이 가져다 쓴다**(sha256 `eb6bef66f9b37550…`). 보상이 평가와 다른 판정을 쓰면 한 과녁을 최적화하고 다른 과녁으로 재는 일이 된다.

**두 성분은 로그까지 분리해서 기록한다.** 스키마 보상이 포화하는 동안 완전 일치 보상이 제자리인 것과 둘 다 오르는 것은 서로 다른 결과이고, 스칼라 하나로는 구분되지 않는다.

## 무엇이 일어났나

첫 10%와 마지막 10% 스텝의 평균이다.

| 지표 | 처음 | 마지막 | 변화 |
|---|---|---|---|
| 보상 합계 | 1.561 | 1.654 | +0.093 |
| 보상 — 스키마 | 0.999 | 1.000 | +0.001 |
| 보상 — 완전 일치 | 0.562 | 0.654 | +0.092 |
| 생성 길이 (토큰) | 12.624 | 12.623 | -0.001 |

### 스텝별 곡선

```
  보상 — 완전 일치             ▂▅▁▃▃▆▅▁▃▃▅▄▃▇▆▇▆▅▃▇▄█▅▄▄▂▇▄▄▇▄▂▂▆▅▁▃▆▇▇▇█▆▆▇▄▆▃▇▅  0.500 → 0.641
  그룹 붕괴 비율               ▁▂▂▃▁▄▅▅▅▅▆▅▅▅▇▆█▇▆▆▄▅▇▆▅█▇▆███▅▇▆██▇▇▇█▇█▇▆██▇▇▇█  0.625 → 0.875
  그룹 내 보상 편차             █▇▇▆█▅▅▄▄▄▃▄▄▄▃▃▁▂▃▄▅▄▂▃▄▁▂▃▁▁▂▄▂▄▁▁▂▂▃▁▂▁▂▃▂▁▂▃▂▂  0.156 → 0.041
  정책 엔트로피                ▇▆█▆▇▅▃▇▄▄▃▃▃▃▂▂▁▂▃▂▂▁▂▂▂▂▁▁▁▁▁▂▁▂▁▂▂▂▁▁▁▁▁▁▁▁▁▂▁▁  0.147 → 0.014
  생성 길이(토큰)              ▄▃▇▅▇▅▅▄▇▇▅▇▆█▄▃▅▃▆▆▇▅▇▁▃▇▂▅▆▂▃▅▄▂▃▄▃▃▅▇▃▁▆▁▄█▄▅▃▆  12.730 → 12.560
```

| 스텝 | 완전 일치 | 그룹 붕괴 | 그룹 내 편차 | 엔트로피 |
|---|---|---|---|---|
| 1–20 | 0.562 | 0.519 | 0.196 | 0.1078 |
| 21–40 | 0.593 | 0.719 | 0.115 | 0.0725 |
| 41–60 | 0.648 | 0.762 | 0.099 | 0.0441 |
| 61–80 | 0.677 | 0.831 | 0.070 | 0.0310 |
| 81–100 | 0.659 | 0.756 | 0.098 | 0.0293 |
| 101–120 | 0.642 | 0.881 | 0.047 | 0.0225 |
| 121–140 | 0.588 | 0.850 | 0.062 | 0.0251 |
| 141–160 | 0.652 | 0.894 | 0.045 | 0.0227 |
| 161–180 | 0.736 | 0.869 | 0.055 | 0.0171 |
| 181–200 | 0.654 | 0.881 | 0.046 | 0.0175 |

**보상은 161–180스텝 구간에서 0.736로 정점을 찍는다.** 그 구간의 그룹 붕괴는 0.869, 마지막 구간에서는 0.881이다. 보상이 정점을 지나 내려오는 것과 붕괴가 올라가는 것은 같은 현상의 두 얼굴이다 — 그룹이 만장일치가 되면 어드밴티지가 0이 되어 그 스텝은 아무것도 가르치지 않고, 남은 기울기는 이미 확신한 방향을 더 뾰족하게 만드는 데만 쓰인다.

### 길이 드리프트

**드리프트 없음.** 평균 생성 길이가 12.6토큰에서 12.6토큰으로 -0.0토큰(-0.0%) 움직였다. 장황해지는 방식의 보상 해킹은 이 실행에서 관측되지 않았다.

상한에 닿은 생성의 최대 비율은 **0.0%**이다. 상한이 길이 측정을 구속하지 않았으므로 위 수치는 드리프트 자체를 읽은 것이다.

### 그룹 붕괴 — 원인은 세 층이다

그룹 안의 롤아웃 여덟 개가 모두 같은 점수를 받아 어드밴티지가 0이 된 비율은 전체 평균 **79.6%**, 처음 구간 51.9%에서 마지막 구간 **88.1%**까지 올라갔다. 그만큼의 스텝이 기울기를 만들지 못했다는 뜻이고, 이 실행의 유효 스텝 수는 명목 스텝 수보다 상당히 작다.

**원인을 하나로 돌리지 않는다.** 세 가지가 같은 방향으로 겹쳐 있고, 이 실행만으로는 각각의 기여를 분리할 수 없다.

**(a) 프롬프트가 출발 체크포인트에게 쉽다.** 풀은 Cond-2가 학습한 파일 그 자체이고(위 출처 절), 처음 구간의 완전 일치 보상이 이미 0.562이다. 이미 아는 문제에 여덟 번 답하면 여덟 개가 같은 답이 되기 쉽다. 이것이 과제 선택 오류가 실제로 나타난 자리다.

**(b) 과제의 출력 엔트로피가 원래 낮다.** 생성물은 `{"cwe_id": "CWE-NNN"}` 형태로 고정되어 있어 실제로 선택이 일어나는 토큰은 사실상 하나다. 정책 엔트로피는 **1스텝에서 이미 0.1474**이며, 학습이 시작되기 전부터 낮다. 단일 결정 분류 과제에서 표본 여덟 개가 갈리려면 모델이 그 하나의 결정에서 실제로 망설여야 하는데, 그런 항목은 많지 않다.

**(c) KL 항이 없다(`beta: 0.0`).** 정책을 기준 모델 근처에 붙들어 두는 힘이 없으므로 분포는 계속 뾰족해진다. 엔트로피가 0.1474에서 0.0175로 8.4배 줄어든 것이 그 궤적이다. 이 설정은 내가 골랐고, 대가는 여기 나타났다.

**(c)만으로 귀속하는 것은 틀린 설명이다.** KL 항을 켰다면 엔트로피 감소는 느려졌겠지만, (a)와 (b)는 그대로 남는다 — 이미 학습한 쉬운 프롬프트에서 단일 결정 과제의 여덟 표본이 갈리게 만들지는 못한다. 반대로 (a)만 고쳤어도 (c)는 남는다. **세 층을 분리하려면 각각을 바꾼 실행이 필요하고, 이 시연은 그것을 하지 않았다.**

결론적으로 **과제 선택은 이 출발점에 대해 적절하지 않았다.** Cond-0 기준으로는 중간 난이도였지만 Cond-2 기준으로는 아니었고, 붕괴율 79.6%는 그 사실의 직접적 측정이다.

## 고보상 출력을 직접 읽었다

스키마와 완전 일치를 모두 받은 생성 4,187건을 모아 두고, 형태를 기계적으로 분류한 뒤 가장 최근 30건을 사람이 읽었다. 보상 곡선은 "검증자를 속이지 않고 풀었는가"에 답하지 못한다. 무엇이 보상을 받았는지 보는 것만이 답한다.

| 출력 형태 | 건수 | 비율 |
|---|---|---|
| bare JSON object, one key | 4,187 | 100.0% |

토큰 길이 중앙값 12, 평균 12.48, 최대 13.

**찾은 것: 없다.** 만점을 받은 생성은 거의 전부 키 하나짜리 JSON 객체 그대로였고, 정답보다 크게 긴 것도 드물었다. 검증자를 만족시키면서 과제를 풀지 않는 경로는 이 실행에서 나타나지 않았다. 이것은 **검증자가 견고하다는 증명이 아니라, 이 실행에서는 시도되지 않았다는 관측**이다.

## 평가

P4 하니스를 **수정 없이** 돌려 얻은 결과를, 같은 시드의 Cond-2와 짝지은 검정으로 비교한다.

| 세트 | Cond-2 | RLVR | 차이 | 95% 구간 | Holm p | 판정 | 짝지은 MDD |
|---|---|---|---|---|---|---|---|
| `cve_to_cwe/eval_post_cutoff/constrained` | 58.0% | 57.8% | -0.3pp | [-0.9, +0.4]pp | 0.482 | 차이 검출되지 않음 | ±0.9pp |
| `cve_to_cwe/eval_post_cutoff/free` | 58.0% | 57.8% | -0.3pp | [-0.9, +0.4]pp | 0.482 | 차이 검출되지 않음 | ±0.9pp |
| `cve_to_cwe/eval_pre_cutoff/constrained` | 66.6% | 66.1% | -0.5pp | [-1.2, +0.1]pp | 0.206 | 차이 검출되지 않음 | ±0.9pp |
| `cve_to_cwe/eval_pre_cutoff/free` | 66.6% | 66.1% | -0.5pp | [-1.2, +0.1]pp | 0.206 | 차이 검출되지 않음 | ±0.9pp |

**어느 세트에서도 차이가 검출되지 않았다.** 사전에 예상한 결과이며, 스텝 200회와 시드 하나로는 짝지은 MDD보다 작은 변화만 만들 수 있다. 점 추정값이 양수인 칸이 있더라도 **효과로 제시하지 않는다.**

## 이 체크포인트를 만들지 못한 시도

위 수치는 세 번째 실행의 것이다. 앞의 두 번은 지우지 않고 남겼다.

### 1차 — 학습 200스텝을 마친 뒤 저장 단계에서 죽었다

종료 코드 137(시그널 9, SIGKILL). 학습 자체는 2.07시간 동안 정상적으로 끝났고, 죽은 지점은 **writing model shards during the inline merge**이다.

**원인**: `merge_and_unload()`가 살아 있는 트레이너 위에 — 가중치, 옵티마이저 상태, 생성 버퍼가 모두 상주한 채로 — 모델 전체 사본을 하나 더 만들고, 샤드를 쓰는 도중 메모리 상한에 걸렸다. 모든 학습 스텝이 끝난 **뒤**였다.

**고친 방식**: 학습 직후 어댑터와 매니페스트를 먼저 쓰고, 그 사이에 실패할 수 있는 작업을 두지 않는다. 병합은 학습 상태가 사라진 별도 프로세스에서 하고, 어댑터 체크포인트를 50스텝마다 남긴다. (영문 원문은 `runs/rlvr/attempts.json`의 `cause`·`fix` 필드에 있다.)

**메모리 상한을 올리지 않았다.** 올려서 통과시키는 것은 결함을 가리는 것이고, 결함은 상한이 낮다는 것이 아니라 **값비싼 산출물을 값싼 것보다 늦게 저장했다**는 것이다. 두 시간이 저장 단계에서 사라졌다. `docs/engineering-rules.md` 규칙 7이 이 사건에서 나왔다.

### 2차 — 102스텝에서 호스트가 재부팅됐다

**이쪽은 이 코드의 결함이 아니다.** 전원 또는 호스트 수준의 사건이었고, 같은 사이트의 리눅스 두 대가 동시에 테일넷에서 사라졌다. 종료 표식은 기록되지 않았다. 근거는 보상 로그가 중간에서 멈춘 것, 아래 부팅 시각이 실행 시작보다 몇 시간 뒤라는 것, 그리고 학습 로그가 있던 `/tmp`가 그 뒤 비어 있었다는 것이다. **로그 파일의 mtime은 근거가 아니다** — 보존하지 않고 복사했으므로, 뒷받침할 수 없는 주장은 하지 않는다.

**복구**: 1차 이후 추가한 50스텝 간격 어댑터 체크포인트에서 재개했다. 옵티마이저·스케줄러·RNG 상태가 복원되었고, 텍스트 로그가 사라진 뒤의 앞 절반 엔트로피 계열은 체크포인트의 `trainer_state.json`에서 복구했다. 재개에 사용한 체크포인트는 `runs/rlvr/trainer/checkpoint-100`이고(학습 매니페스트의 `resumed_from`에 기록), 호스트 부팅 시각은 `2026-09-21T22:52:46`이다. 현재 디스크에 남아 있는 체크포인트는 `checkpoint-150`, `checkpoint-200`이며, 이는 재개 이후 새로 쓰이고 오래된 것이 정리된 결과다 — 복구 시점의 목록이 아니다.

**1차의 수정이 2차를 구했다.** 50스텝 간격 어댑터 체크포인트는 저장 순서 결함을 고치며 넣은 보험이었고, 그것이 없었다면 정전으로 두 번째 실행도 통째로 사라졌을 것이다.

두 시도의 보상 로그는 `runs/rlvr_attempt1/`과 `runs/rlvr_attempt2_partial/`에 있다.

## 보이지 못한 것

- **성능 향상.** 위 검정이 뒷받침하지 않는다. 이 시연의 목적도 아니다.
- **수렴.** 스텝 200회는 GRPO가 수렴했는지 판단할 수 있는 길이가 아니다.
- **다른 과제로의 일반화.** `cvss_vector`와 `structured_extract`는 베이스 정확도가 낮아 그룹이 대부분 전부-틀림으로 붕괴한다. 이 구조가 그 과제들에서도 도는지는 측정하지 않았다.
- **검증자의 견고성.** 고보상 표본에서 해킹이 관측되지 않았다는 것은 시도되지 않았다는 뜻이지 불가능하다는 뜻이 아니다.
- **재현성.** 롤아웃은 온도를 0보다 크게 두므로 이 실행은 바이트 단위로 재현되지 않는다. 시드·데이터 순서·체크포인트 해시는 기록되어 있으나, 평가 쪽 Gate 4와 같은 종류의 보장은 여기에 없다.

